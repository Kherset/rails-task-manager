<div class='container'>

<h1>My tasks</h1>

<ul>
    <% @tasks.each do |task| %>
  <li>  <%= link_to task.title, task_path(task) %> </li>
    <% end %>
</ul>

<button> <%= link_to "Add a new task", new_path %> </button>

</div>
